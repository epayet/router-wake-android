# Leftover from v0, before setup moved in-app (see README) — a stray local
# copy would contain real FritzBox credentials, so keep ignoring it.
app/src/main/java/com/jakspinning/wakemypc/Config.kt

# Gradle / Android build output
.gradle/
build/
app/build/
local.properties
captures/
.cxx/

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store

# Signing (if ever added)
*.jks
*.keystore
